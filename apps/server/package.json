{
  "name": "server",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server",
    "dev": "cd ../../ && bun --watch apps/server/src/index.ts",
    "start": "bun run dist/index.mjs"
  },
  "dependencies": {
    "@hono/trpc-server": "^0.4.0",
    "@steipete/bird": "^0.8.0",
    "@trend-x/api": "workspace:*",
    "@trend-x/db": "workspace:*",
    "@trend-x/env": "workspace:*",
    "@trpc/server": "catalog:",
    "cron": "^4.4.0",
    "dotenv": "catalog:",
    "drizzle-orm": "^0.45.1",
    "hono": "catalog:",
    "ml-kmeans": "^7.0.0",
    "openai": "^6.17.0",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@trend-x/config": "workspace:*",
    "@types/bun": "catalog:",
    "tsdown": "^0.16.5",
    "typescript": "catalog:"
  }
}
